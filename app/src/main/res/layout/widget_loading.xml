<?xml version="1.0" encoding="utf-8"?>
<!-- Initial and preview layout for the home-screen widget. The launcher
     inflates it through RemoteViews: no app theme, no window background, so
     the background here is the only one painted (Overdraw does not apply). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/widget_bg"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="16dp"
    tools:ignore="Overdraw">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@color/widget_text"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/widget_loading"
        android:textColor="@color/widget_text_dim"
        android:textSize="12sp" />
</LinearLayout>
