<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>Birdo VPN Tunnel</string>
    <!-- CFBundleName is REQUIRED for an app extension: installd rejects the
         host app outright ("does not have a CFBundleName key with a non-zero
         length string value") if the appex omits it. -->
    <key>CFBundleName</key>
    <string>PacketTunnel</string>
    <key>CFBundleIdentifier</key>
    <string>app.birdo.vpn.tunnel</string>
    <!-- MUST resolve to the same values as the host app's Info.plist: App Store
         validation rejects a bundle whose appex version differs from its host.
         Both now read the same build settings, so they cannot drift. -->
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>XPC!</string>
    <key>CFBundleExecutable</key>
    <string>PacketTunnel</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.networkextension.packet-tunnel</string>
        <key>NSExtensionPrincipalClass</key>
        <string>PacketTunnel.PacketTunnelProvider</string>
    </dict>
</dict>
</plist>
